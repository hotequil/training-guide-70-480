const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a / b; //i put a division sign instead of a multiplication one to see the error being triggered by QUnit
const sayHelloWorld = () => "Hello world";